<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
             x:Class="AppTripEver.Views.ServicesView"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:AppTripEver.Controls;assembly=AppTripEver"
             mc:Ignorable="d"
             BackgroundColor="#1A3161"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.ToolbarItems>
        <ToolbarItem 
                 IconImageSource="billetera.png"
                 Order="Primary"
                 Text="Cerrar Sesión"
                 Priority="0" />
        <ToolbarItem 
                 IconImageSource="usuario.png"
                 Order="Primary"
                 Priority="0"
                 />
    </ContentPage.ToolbarItems>
    <Grid Padding="20" RowSpacing="20">
        <Label      
            Grid.Row="0"
            Grid.ColumnSpan="2"
            FontAttributes="Bold"
            FontSize="Large"
            Text="{Binding Usuario.Nombre}"
            FontFamily="Lobster"
            TextColor="White"                
            VerticalOptions="CenterAndExpand" 
            HorizontalOptions="CenterAndExpand" />
        
        <Frame CornerRadius="5" BackgroundColor="#393c3e" Grid.Row="1" Padding="0" Margin="0,0,0,0">
            <Button
            BackgroundColor="#393c3e"
            Text="Crear Usuario"
            TextColor="White"
            Command="{Binding CrearHostCommand}"/>
        </Frame>
    </Grid>
</ContentPage>